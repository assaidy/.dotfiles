#!/bin/bash
# Create a new tmux session
tmux new-session -d -s hope

# Rename the default window to 'code'
tmux rename-window -t hope:0 code

# Create another new window named 'comp'
tmux new-window -n comp -t hope:

# Attach to the new session
tmux attach-session -t hope

